import 'package:flutter/material.dart';

class UnknownPageView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Pagina não encontrada')),
      ),
    );
  }
}
